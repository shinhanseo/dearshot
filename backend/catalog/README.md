# Development catalog fixtures

`seed.json` and every asset below this directory are development-only fixtures. They verify the catalog contract, import transaction, localization fallback, asset delivery, and Android integration before the real scene taxonomy, compositions, copy, and overlays are curated.

Do not treat these samples as approved launch content. Replace them through a reviewed catalog manifest; do not edit a published template version in place.
